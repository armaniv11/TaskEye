import 'package:flutter/material.dart';

class DefaultFloatingButtonHeight extends StatelessWidget {
  const DefaultFloatingButtonHeight({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 60,
    );
  }
}
